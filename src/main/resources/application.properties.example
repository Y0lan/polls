## Propriété du serveur
server.port= 8080

## Database

spring.datasource.url= jdbc:mysql://localhost:3306/polls?useSSL=false&serverTimezone=Etc/UTC&allowPublicKeyRetrieval=true
spring.datasource.username= root
spring.datasource.password= <mysql_password>
spring.datasource.initialization-mode=always
# Automatique update des tables dans la database
spring.jpa.hibernate.ddl-auto = update

## Propriété Hibernate

# Hibernate + MYSQL  optimisation
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

## Logging
logging.level.org.hibernate.SQL= DEBUG

## Jackson
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
spring.jackson.time-zone= ECT

## JWT
app.jwtSecret= <jwt-secret>

# One week
app.jwtExpirationInMs= 604800000
