
version: 2.1
orbs:
  docker: circleci/docker@1.5.0
  maven: circleci/maven@1.2.0
jobs:
  build:
    docker:
      - image: docker
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: docker-compose up -d
          command: |
            docker-compose up
            
workflows:
  maven_test:
    jobs:
      - maven/test
